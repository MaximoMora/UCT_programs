
def minimun_values():
    list_user = []
    exit_user = False
    while exit_user != True:
        value_user = int(input("ingrese un numero: "))
        list_user.append()

    minimum_value = list[1]
    value = 0
    list_minimum = []
    for i in range(0,len(list)):
        value = list[i]
#        print(value)
        if value <= minimum_value:
            minimum_value = value
            list_minimum.append(minimum_value)
    print(f"los valores minimos de la lista son: {list_minimum}")





